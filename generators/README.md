# Generators

Generation logic lives in `src/reverse_vibecoding`.
This directory is reserved for declarative generation manifests, fixture inputs, and future generator adapters.

The CLI is intentionally not implemented here.

