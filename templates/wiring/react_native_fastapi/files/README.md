# React Native + FastAPI Wiring

Run the backend first, then start the mobile app with `EXPO_PUBLIC_API_URL` pointing to the backend.

For Android emulators, `localhost` may need to be `http://10.0.2.2:8000`.
